step 1: Make sure to Create a format Structure-->
sentiment_app/
│
├── app.py                  # Main Flask backend
├── templates/
│     └── index.html        # Frontend HTML UI
├── static/
│     └── style.css         # (Optional) custom styles
└── requirements.txt        # Python dependencies

step 2: Create and copy all the format in proper way(neccessary) -->

step 3: now open terminal and run the following-->
        ----> pip install -r requirements.txt  

        ----> python app.py

        (Finished Click on the link http://127.0.0.1:5000)